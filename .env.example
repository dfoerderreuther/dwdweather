# MapTiler key for the stations map (https://cloud.maptiler.com/account/keys/).
# The key must allow the origin you serve from (e.g. http://localhost:5173 for dev
# and your production domain). Copy this file to `.env` and set your key:
VITE_MAPTILER_KEY=
